name: protobuf-storage
version: 0.3.5

authors:
  - maiha <maiha@wota.jp>

dependencies:
  protobuf:
    # github: jeromegn/protobuf.cr
    # use fork for Protobuf::Message[](key)
    github: maiha/protobuf.cr
    branch: hash-accessors

  pretty:
    github: maiha/pretty.cr

  var:
    github: maiha/var.cr

development_dependencies:
  spec2:
    # github: waterlink/spec2.cr
    # use fork for crystal-0.33 or higher
    github: maiha/spec2.cr
    version: ">= 0.12.0"

license: MIT
